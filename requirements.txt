# Market Regime Oracle — pinned dependencies
# Python 3.11+
requests==2.34.2
numpy==2.4.6
pandas==3.0.3
matplotlib==3.11.0
mcp==1.27.1

# --- test/dev only (needed by the `docker compose run tests` service) ---
pytest==9.1.1
